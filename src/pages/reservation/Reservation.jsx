import React from 'react';
import styled from 'styled-components';

const Reservation = () => {
  return <ReservationContainer></ReservationContainer>;
};

const ReservationContainer = styled.div``;
export default Reservation;
